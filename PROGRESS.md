# PROGRESS.md

> Per Lecture 05. The minimum human content the next session needs:
> where things are, what's broken, and the next best step. **Feature
> status is not duplicated here** — `features/*.json` is the source of
> truth (run `npm run harness:features` for the one-line summary).
> Decisions go in `DECISIONS.md`; per-branch session notes go in the
> PR description.

## Current state

- Trunk: `main` at PR #7 merge (`2f58414`)
- Baseline: `npm run verify` green

## Known issues

- None.

## Next steps

1. Land this PR (`chore/harness-progress`).
2. Pick up `ui-home` — the first user-facing feature.
